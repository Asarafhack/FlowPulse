import {createFileRoute} from '@tanstack/react-router';import {Protected} from '@/components/layout/protected';import {useAuth} from '@/lib/auth';export const Route=createFileRoute('/profile')({component:Profile});function Profile(){const {user}=useAuth();return <Protected><div className="max-w-2xl space-y-6"><div><h1 className="text-3xl font-bold">Profile</h1><p className="mt-1 text-sm text-slate-500">Your authenticated account details.</p></div><div className="rounded-xl border bg-white p-6"><div className="space-y-5">{[['Full name',user?.fullName],['Email',user?.email],['Role',user?.role],['Created',user?.createdAt?new Date(user.createdAt).toLocaleString():'—']].map(([a,b])=><div key={a as string}><div className="text-xs font-medium uppercase tracking-wide text-slate-400">{a}</div><div className="mt-1 font-medium">{b}</div></div>)}</div></div></div></Protected>}
