import {createFileRoute} from '@tanstack/react-router';import {Protected} from '@/components/layout/protected';import {useDashboardStats} from '@/lib/queries';import {FolderKanban,CheckSquare,Circle,Clock3,AlertTriangle} from 'lucide-react';
export const Route=createFileRoute('/dashboard')({component:Dashboard});function Dashboard(){const {data,isLoading,error}=useDashboardStats();return <Protected><div className="space-y-7"><div><h1 className="text-3xl font-bold tracking-tight">Dashboard</h1><p className="mt-1 text-sm text-slate-500">A live overview of your projects and tasks.</p></div>{error&&<div className="rounded-lg border border-red-200 bg-red-50 p-4 text-sm text-red-700">Unable to load dashboard. Please refresh or sign in again.</div>}<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{[['Projects',data?.totalProjects,FolderKanban],['Tasks',data?.totalTasks,CheckSquare],['Completed',data?.completedTasks,CheckSquare],['Pending',data?.pendingTasks,Clock3],['In progress',data?.projectsInProgress,Circle]].map(([label,value,Icon]:any)=><div className="rounded-xl border bg-white p-5 shadow-sm" key={label as string}><div className="flex items-center justify-between"><span className="text-sm text-slate-500">{label}</span><Icon size={18} className="text-slate-400"/></div><div className="mt-3 text-3xl font-bold">{isLoading?'—':value}</div></div>)}</div>{data&&<div className="grid gap-4 lg:grid-cols-3"><section className="rounded-xl border bg-white p-6 lg:col-span-2"><h2 className="font-semibold">Project status</h2><div className="mt-5 space-y-4">{Object.entries(data.projectsByStatus).map(([k,v])=><div key={k}><div className="mb-1 flex justify-between text-sm"><span>{k.replaceAll('_',' ')}</span><b>{v}</b></div><div className="h-2 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-slate-900" style={{width:`${data.totalProjects?Math.max(4,(v/data.totalProjects)*100):0}%`}}/></div></div>)}</div></section><section className="rounded-xl border bg-white p-6"><h2 className="font-semibold">Task health</h2><div className="mt-5 space-y-4 text-sm"><div className="flex justify-between"><span>High priority</span><b>{data.tasksByPriority.HIGH}</b></div><div className="flex justify-between"><span>In progress</span><b>{data.inProgressTasks}</b></div><div className="flex justify-between text-amber-700"><span className="flex items-center gap-2"><AlertTriangle size={16}/>Overdue</span><b>{data.overdueTasks}</b></div></div></section></div>}</div></Protected>}
