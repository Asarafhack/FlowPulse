import {Link,useNavigate} from '@tanstack/react-router'; import {useEffect,useState} from 'react'; import {useAuth} from '@/lib/auth'; import {ApiError,fieldErrorMap,errorMessage} from '@/lib/api-client';
export function AuthLayout({register=false}:{register?:boolean}){const {status,login,register:signup}=useAuth();const nav=useNavigate();const [form,setForm]=useState({fullName:'',email:'',password:''});const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [fields,setFields]=useState<Record<string,string>>({});useEffect(()=>{if(status==='authenticated')nav({to:'/dashboard'});},[status,nav]);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError('');setFields({});try{if(register)await signup(form);else await login({email:form.email,password:form.password});nav({to:'/dashboard'});}catch(err){setError(errorMessage(err));setFields(fieldErrorMap(err));}finally{setBusy(false)}};return <div className="grid min-h-screen place-items-center bg-gradient-to-br from-slate-100 via-white to-slate-100 p-4"><div className="w-full max-w-md rounded-2xl border bg-white p-7 shadow-xl"><div className="mb-7 text-center"><div className="mx-auto mb-3 grid h-12 w-12 place-items-center rounded-xl bg-slate-900 text-white">FP</div><h1 className="text-2xl font-bold tracking-tight">{register?'Create your account':'Welcome back'}</h1><p className="mt-1 text-sm text-slate-500">{register?'Start organizing projects and tasks.':'Sign in to your workspace.'}</p></div>{error&&<div className="mb-4 rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}<form onSubmit={submit} className="space-y-4">{register&&<Field label="Full name" value={form.fullName} error={fields.fullName} onChange={v=>setForm({...form,fullName:v})}/>}<Field label="Email" type="email" value={form.email} error={fields.email} onChange={v=>setForm({...form,email:v})}/><Field label="Password" type="password" value={form.password} error={fields.password} onChange={v=>setForm({...form,password:v})}/><button disabled={busy} className="w-full rounded-lg bg-slate-900 py-2.5 text-sm font-semibold text-white disabled:opacity-60">{busy?'Please wait…':register?'Create account':'Sign in'}</button></form><p className="mt-6 text-center text-sm text-slate-500">{register?'Already have an account? ':'New here? '}<Link className="font-semibold text-slate-900 underline" to={register?'/login':'/register'}>{register?'Sign in':'Create an account'}</Link></p></div></div>}
function Field({label,type='text',value,error,onChange}:{label:string;type?:string;value:string;error?:string;onChange:(v:string)=>void}){return <label className="block"><span className="mb-1.5 block text-sm font-medium">{label}</span><input type={type} value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-lg border px-3 py-2.5 outline-none focus:border-slate-900 focus:ring-2 focus:ring-slate-200" required/>{error&&<span className="mt-1 block text-xs text-red-600">{error}</span>}</label>}
